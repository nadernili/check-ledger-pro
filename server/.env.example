PORT=5050
DB_FILE=./data.sqlite

# Email (Nodemailer SMTP) — optional; leave blank to disable reminders
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
FROM_EMAIL="Check Ledger <no-reply@example.com>"
NOTIFY_TO=
